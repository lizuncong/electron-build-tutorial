name: Release

on:
  push:
    branches:
      - feature/forge
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest, macos-latest]
        arch: ['x64', 'arm64']
    steps:
    - uses: actions/checkout@v4
    - name: Install dependencies
      run: npm install
    - name: publish
      run: |
          npx electron-forge publish --arch=${{ matrix.arch }} --targets=all
      env:
        GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
        ELECTRON_ARCH: ${{ matrix.arch }} 
